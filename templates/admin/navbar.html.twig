<!-- Left navbar links -->
    <ul class="navbar-nav">
      <li class="nav-item">
        <a class="nav-link" data-widget="pushmenu" href="#"><i class="fas fa-bars"></i></a>
      </li>

    </ul>

    <ul class="navbar-nav ml-auto">
      
      {%  if app.user %}
      <li class="nav-item d-none d-sm-inline-block">
        <a href="{{ path('user_myaccount') }}" class="nav-link">{{ 'Security.actions.myAccount'|trans() }} ({{ app.user.username }})</a>
      </li>
      <li class="nav-item d-none d-sm-inline-block">
        <a href="{{ path('logout') }}" class="nav-link"><i class="fas fa-sign-out-alt"></i> {{ 'Security.actions.logout'|trans() }}</a>
      </li>
      {% endif %}
    </ul>



