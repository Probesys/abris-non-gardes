{% extends 'admin/base-admin.html.twig' %}

{% block title %}
   {{ 'Entities.HelpMessage.actions.create_new'|trans }}
{% endblock title %}

{% block breadcrumb %}
   <li class="breadcrumb-item"><a href="{{ path('helpMessage_index') }}">{{ 'Entities.helpMessage.title'|trans }}</a></li>
   <li class="breadcrumb-item active">{{ 'Entities.helpMessage.actions.create_new'|trans }}</li>
{% endblock breadcrumb %}

{% block body %}

   <div class="col-md-12">
   {{ form_start(form) }}
   {{ form_widget(form) }}
   </div>
   <div class="btn-group mt-4">
      <a class="btn btn-default" data-toggle="tooltip" data-placement="top" title="{{ 'Generics.actions.backToTheList'|trans() }}" data-container="body" href="{{ path('helpMessage_index') }}">
         <i class="fas fa-list"></i>&nbsp;<span>{{'Generics.actions.backToTheList'|trans() }}</span>
      </a>
      <input type="submit" class="btn btn-primary" value="{{ 'Generics.actions.create'|trans() }}">
      <input type="submit" class="btn btn-info" name="createAndContinue" value="{{ 'Generics.actions.createAndContinue'|trans() }}">
   </div>
   {{ form_end(form) }}

{% endblock %}
